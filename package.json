{
  "name": "gutu-lib-ui-kit",
  "private": true,
  "type": "module",
  "workspaces": [
    "framework/libraries/*"
  ],
  "scripts": {
    "build": "bun run --cwd framework/libraries/ui-kit build",
    "typecheck": "bun run --cwd framework/libraries/ui-kit typecheck",
    "lint": "bun run --cwd framework/libraries/ui-kit lint",
    "test": "bun run --cwd framework/libraries/ui-kit test",
    "test:unit": "bun run --cwd framework/libraries/ui-kit test:unit",
    "docs:summary": "node scripts/docs-summary.mjs",
    "docs:check": "node scripts/docs-check.mjs"
  }
}
